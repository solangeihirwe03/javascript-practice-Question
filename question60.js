function perimeterOfRectangle(width, height) {
   
    return 2 * (width + height);
  }
  

  const rectangleWidth = 5;
  const rectangleHeight = 3;
  const perimeter = perimeterOfRectangle(rectangleWidth, rectangleHeight);
  console.log("Perimeter of the rectangle:", perimeter);