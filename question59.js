function areaOfRectangle(width, height) {
    
    return width * height;
  }
  

  const rectangleWidth = 5;
  const rectangleHeight = 3;
  const area = areaOfRectangle(rectangleWidth, rectangleHeight);
  console.log("Area of the rectangle:", area);